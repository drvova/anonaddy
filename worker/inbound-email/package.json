{
  "name": "vovamail-inbound-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "setup": "bash ./scripts/setup.sh",
    "setup-dns": "bash ./scripts/setup-dns.sh"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
